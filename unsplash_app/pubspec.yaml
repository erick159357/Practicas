name: unsplash_app
description: "Proyecto final - App de wallpapers con Unsplash. Erick Garcia Vega"
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  dio: ^5.7.0
  shared_preferences: ^2.5.3
  connectivity_plus: ^6.1.0
  cached_network_image: ^3.4.1
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
